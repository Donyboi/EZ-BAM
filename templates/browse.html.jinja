<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <title>EZ-BAM</title>
  </head>

  <body class="bg-gradient-to-b from-gray-700 to-gray-500 shadow-md px-6 py-3 text-gray-400 min-h-screen font-sans">

   
    {% include 'components/navbar.html.jinja' %}


     
      </p>

      
      </div>
    </section>
    
    {%for message in get_flashed_messages() %}
    <li>{{ message }}</li>
    

    {%endfor%}
    <main class = "grid grid-cols-4 gap-3 max-w-7xl mx-auto py-6 mb-5">
      {%for x in products %}
        
        <a href="/product/{{x ['ID'] }}" class="grid place-items-center bg-gray-800 p-4 rounded-lg hover:scale-105 hover:bg-gray-700 transition-all duration-200">
          <img class="h-25 mx-auto" src = "{{ x['Image'] }}" >
          <div>{{x["Name"]}}</div>
          <div class="text-right font-bold text-sm">$ {{x["Price"]}}</div>
          </a>
      {%endfor%}

      
    </main>
  </body>
</html>
